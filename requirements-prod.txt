# Dependencias SOLO del servicio que se despliega.
#
# Diferencia con requirements.txt: aqui no va pandas. El servicio no lo usa,
# solo los scripts de entrenamiento. Quitarlo ahorra ~60 MB de imagen y
# memoria, que en el plan gratuito de Render (512 MB) si se nota.
#
# scipy no se lista porque scikit-learn lo instala como dependencia.

fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
python-multipart==0.0.32
numpy==2.5.3
soundfile==0.14.0
scikit-learn==1.9.1
joblib==1.6.0
