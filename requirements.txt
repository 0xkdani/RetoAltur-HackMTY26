# Versiones fijas a proposito.
#
# El modelo entrenado (models/fusion.joblib) lo serializa scikit-learn.
# Si cada quien instala una version distinta, al cargarlo salta un aviso
# de incompatibilidad y en el peor caso da predicciones raras. Con un
# equipo de varias personas eso es cuestion de tiempo, no de suerte.
#
# Verificado el 2026-09-12: todas tienen paquetes para Python 3.12, 3.13
# y 3.14 en Windows, asi que no importa que version use cada quien.
# Lo que si importa es que numpy y scipy piden Python 3.12 o mas nuevo.

fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
numpy==2.5.3
soundfile==0.14.0
scikit-learn==1.9.1
pandas==3.0.5
joblib==1.6.0
python-multipart==0.0.32
